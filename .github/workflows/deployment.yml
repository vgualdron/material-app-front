name: Build
on: 
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.18.1]
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credential: false

      - name: Install and Build 🔧
        run: |
          npm --openssl-legacy-provider install
          npm --openssl-legacy-provider run build
      - name: Sync files 📂 Server flamecol.com
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ftp://154.49.247.25
          username: u489681413.ftpuserflame
          password: Flame1234
          dangerous-clean-slate: true
          log-level: verbose
          protocol: ftps
          timeout: 120000
          local-dir: ./dist/spa/
